# VoiceInk 开发与交付流程

本文件适用于整个仓库。所有由 Agent 执行的功能开发、缺陷修复、性能优化、模型接入和发布任务，都必须按以下流程闭环。除非用户明确缩小范围，否则不能停在“代码已修改”或“分支已推送”。

标准流程：

`方案设计 → 开发 → 测试 → 提交代码 → 处理 CI → 合入主干 → 发布版本 → 本机安装 → 验收`

## 总体原则

- 先核对事实再修改：检查当前分支、工作树、远端主干、现有实现、测试、运行状态和发布状态。
- 保护用户现场：不得覆盖或提交不属于当前任务的修改；工作树有无关改动时，使用独立 worktree，或先请求用户确认范围。
- 默认从最新 `origin/main` 创建 `codex/<简短任务名>` 分支，不直接在 `main` 上开发或提交。
- 只做任务范围内的必要变更。需要扩大功能、改变数据、破坏兼容性或执行不可恢复操作时，先说明影响并取得授权。
- 不把猜测写成结论。性能、准确率、兼容性、CI、发布和安装状态都必须有当前证据。
- 禁止把密码、Token、证书、个人路径中的敏感内容或真实用户数据提交到仓库。
- 阶段失败时先定位原因并修复；不能跳过失败步骤后继续宣称交付完成。

## 1. 方案设计

开发前必须形成可执行方案，至少包含：

1. 用户目标和可观察的验收标准。
2. 当前实现与问题证据，包括相关文件、调用链、运行方式和已知限制。
3. 变更边界：要修改什么、明确不修改什么。
4. 技术方案、备选方案和选择理由。
5. 对兼容性、隐私、性能、资源占用、离线能力、数据迁移和回滚的影响。
6. 测试计划、发布计划和安装验收计划。

涉及 ASR 模型或运行时的方案还必须明确：

- 模型来源、固定 revision、许可证、体积和下载方式。
- 支持语言与方言，不得用“多语言”代替明确范围。
- 推理后端是 CPU、Metal GPU、ANE 还是云端。
- “原生流式、滑动窗口、分段离线”必须准确区分。
- 首字延迟、最终结果延迟、RTF、内存、功耗、文本回写率和静音幻觉等评测指标。
- 模型列表、选择、下载、删除、预热、运行和降级路径是否完整。

方案确定后再开始修改代码。简单且低风险的变更可以把方案写在工作记录或 PR 描述中，不强制新建设计文档。

## 2. 开发

- 先同步远端：`git fetch origin`，确认本地主干可安全 fast-forward，再从最新主干创建任务分支。
- 阅读目标代码及其测试，沿用现有架构、命名、Swift 风格和依赖管理方式。
- 保持提交聚焦；不要顺手格式化、重构或清理无关代码。
- 新功能必须同步补齐必要的错误处理、状态展示、可观测信息和用户可恢复路径。
- 新增外部依赖必须固定版本或 revision，并验证来源与校验机制。
- 下载型功能必须支持失败重试、进度、完整性检查以及未下载状态，不能把大模型直接塞进应用发布包，除非方案明确要求。
- 修改发布、安装、权限、自动更新或模型运行时时，要验证真实产物，不能只验证 mock。

## 3. 开发测试

### 基础检查

每个改动至少执行：

```bash
git diff --check
xcodebuild test \
  -project VoiceInk.xcodeproj \
  -scheme VoiceInk \
  -destination 'platform=macOS,arch=arm64' \
  -only-testing:VoiceInkTests \
  -parallel-testing-enabled NO \
  CODE_SIGNING_ALLOWED=NO \
  ENABLE_TESTABILITY=YES
```

根据变更范围追加：

- UI 行为：运行相关 `VoiceInkUITests`，并人工核对关键界面和状态转换。
- 构建或依赖变化：运行 `make check`、相关构建命令以及必要的 clean build。
- ASR 流式变化：运行 `scripts/run-asr-streaming-benchmark.sh`，并使用真实下载模型和真实音频补充端到端测试。
- 发布变化：在临时输出目录运行 `scripts/release.sh --version <版本> --build-number <编号> --output-dir <目录>`。

测试报告必须说明实际执行的命令、通过数量和跳过项。因环境限制无法执行的测试必须明确标为未验证，不能等同于通过。

性能数据必须同时记录机器型号、芯片、内存、模型版本/量化、音频长度、chunk 配置、冷/热启动和统计口径。不同数据集上的 WER/CER 或产品评分不得混写为同一个“准确率”。

## 4. 提交代码

提交前必须：

1. 执行 `git status --short --branch`，确认分支和文件范围。
2. 阅读完整 diff，执行 `git diff --check`。
3. 只显式暂存当前任务文件，不默认使用 `git add -A`。
4. 再检查 staged diff，确认没有构建产物、模型文件、凭证或无关修改。
5. 使用简短、可追踪的 commit message。

随后推送任务分支并创建 PR。维护者任务仍通过 PR 合入；不要把仓库现有 PR 模板中面向外部贡献者的“关闭 PR”提示复制到维护 PR 描述中。

PR 描述至少包含：

- 变更内容和原因。
- 用户可见影响与兼容性。
- 风险及回滚方式。
- 已执行的测试与结果。
- 性能或准确率数据的测试口径。
- 截图、日志或产物链接（适用时）。

## 5. 处理 CI

- 以 GitHub 上当前 PR 的权威状态为准，等待所有必需检查进入终态。
- CI 失败时先读取失败 job 和日志，判断是代码问题、测试问题、依赖问题还是基础设施瞬时故障。
- 代码或测试问题必须修改并重新验证后推送；只有证据表明是瞬时故障时才直接 rerun。
- 同时处理 reviewer 的有效意见和未解决线程；修改后重新运行受影响测试。
- 没有配置 PR checks 不等于“CI 全绿”。此时必须明确说明“当前 PR 无远端检查”，并把本地验证结果写入 PR。
- 合入前确认 PR `mergeable`，分支没有落后导致的冲突，且最新 commit 与已验证 commit 一致。

## 6. 合入主干

- 只有在本地验证完成、CI 达到可接受终态、review 意见处理完毕后才能合入。
- 默认使用 squash merge，保持主干历史聚焦；仓库或用户另有要求时遵从其要求。
- 合入后删除远端任务分支，执行 `git fetch origin main`，并验证 merge commit 已在 `origin/main`。
- 不得用本地“测试通过”代替远端“代码已经合入”的证明。
- 合入后若发现问题，使用新提交修复；不得重写已经共享的主干历史。

## 7. 发布版本

仅在用户要求发布，或任务终态明确包含发布时执行。

1. 从最新 `origin/main` 选择符合 SemVer 的新版本 `X.Y.Z`。
2. 确认版本对应 commit 已在主干，工作树无未提交变更。
3. 高风险发布先在本地运行 release 脚本并验证产物。
4. 创建指向主干 commit 的 `vX.Y.Z` tag 并推送。
5. 监控 `.github/workflows/release.yml` 到终态，不能在 job 仍运行时宣称发布完成。
6. 验证 GitHub Release 不是 draft/prerelease，并包含：
   - `VoiceInk-arm64.zip`
   - `VoiceInk-x86_64.zip`
   - `SHA256SUMS`
   - `voiceink.rb`
7. 重新下载线上资产，执行 SHA256 校验，解压并验证版本、build、bundle ID、各包仅包含对应 CPU 架构、代码签名和必需资源。

已发布且已有用户可下载资产的 tag 不得移动或覆盖；需要修复时发布新的 patch 版本。若 tag 触发错误但尚未生成 Release，任何取消、删除或重建 tag 的操作都必须先核对 workflow 与远端 Release 状态并留下说明。

## 8. 本机安装版本

只有用户明确要求安装时才修改 `/Applications`：

1. 查询 GitHub 最新正式 Release，确认目标版本。
2. 根据本机 CPU 下载 `VoiceInk-arm64.zip` 或 `VoiceInk-x86_64.zip` 以及 `SHA256SUMS`，校验通过后再解压。
3. 验证待安装应用的版本、build、架构、签名和必需资源。
4. 若 VoiceInk 正在运行，先正常退出并确认进程结束。
5. 将现有 `/Applications/VoiceInk.app` 保留为可回滚副本，再安装新版本。
6. 安装后从 `/Applications/VoiceInk.app` 启动。
7. 验证实际运行进程路径、版本、build、架构和关键运行时资源。
8. 验证成功后把旧版本移到废纸篓并告知用户仍可恢复；失败则立即回滚。

不要把“ZIP 已下载”视为“本机已安装”，也不要把“应用可启动”视为功能验收完成。

## 9. 验收与交付报告

验收必须回到方案中的用户可观察标准，至少覆盖：

- 核心功能的成功路径。
- 下载失败、模型缺失、权限不足、离线、静音或输入异常等关键失败路径。
- 升级后原有配置和数据是否保留。
- 实际使用的是目标模型、目标后端和目标流式模式，而不是回退路径。
- 长时间运行、内存增长、温度或功耗等与任务相关的稳定性指标。

最终报告必须明确列出：

- 最终结果和用户可见变化。
- 分支、commit、PR、主干合入状态。
- CI/测试命令及结果。
- Release 版本、workflow 和线上资产验证结果。
- 本机安装位置、运行版本和验收结果。
- 未验证项、已知限制、风险与回滚方式。

只有用户要求范围内的所有阶段都完成，且相应证据已核验，才能宣称任务完成。
